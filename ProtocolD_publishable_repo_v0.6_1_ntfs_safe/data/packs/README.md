# data/packs

This folder contains **Windows-safe short-named** derived dipole packs.

Mapping between the short IDs and the original full filenames is in:
- `data/pack_ids.csv`

The original full-named pack files are archived as:
- `data/packs_full.zip`
